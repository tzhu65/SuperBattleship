var React = require('react');

var Canvas = React.createClass({

  render: function() {
    return (
      <div id="bs-grid">
        <canvas id="bs-canvas"></canvas>
      </div>
    )
  }

});

module.exports = Canvas;
